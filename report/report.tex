\documentclass[11pt,a4paper]{article}

% ---------- Page geometry ----------
\usepackage[a4paper,top=0.75in,bottom=0.8in,left=0.75in,right=0.75in,
            headsep=8pt,footskip=22pt]{geometry}

% ---------- Fonts ----------
\usepackage{newtxtext}
\usepackage{newtxmath}
\usepackage[T1]{fontenc}

% ---------- Microtype ----------
\usepackage{microtype}

% ---------- Line spacing ----------
\linespread{0.97}

% ---------- Paragraph spacing ----------
\setlength{\parskip}{2pt plus 1pt minus 0.5pt}
\setlength{\parindent}{1em}

% ---------- Section heading spacing ----------
\usepackage[compact]{titlesec}
\titlespacing*{\section}      {0pt}{6pt plus 1pt minus 1pt}{2pt}
\titlespacing*{\subsection}   {0pt}{4pt plus 1pt minus 1pt}{1pt}
\titlespacing*{\subsubsection}{0pt}{3pt plus 1pt minus 1pt}{1pt}
\titlespacing*{\paragraph}    {0pt}{2pt}{0.6em}
\titleformat*{\section}{\large\bfseries}
\titleformat*{\subsection}{\normalsize\bfseries} 
\titleformat*{\subsubsection}{\normalsize\itshape}

% ---------- Float & caption spacing ----------
\setlength{\textfloatsep}{6pt plus 2pt minus 2pt}
\setlength{\intextsep}{5pt plus 2pt minus 2pt}
\setlength{\floatsep}{5pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{3pt}
\setlength{\belowcaptionskip}{0pt}

% ---------- Math display spacing ----------
\setlength{\abovedisplayskip}{3pt}
\setlength{\belowdisplayskip}{3pt}
\setlength{\abovedisplayshortskip}{2pt}
\setlength{\belowdisplayshortskip}{2pt}

% ---------- Tables ----------
\renewcommand{\arraystretch}{0.95}
\setlength{\tabcolsep}{4pt}

% ---------- Widow/orphan ----------
\clubpenalty=9999
\widowpenalty=9999

% ---------- Packages ----------
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{amsmath}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{decorations.pathreplacing}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}

\definecolor{csvcolor}{HTML}{E8F5E9}
\definecolor{parsecolor}{HTML}{FFF3E0}
\definecolor{colcolor}{HTML}{E3F2FD}
\definecolor{enginecolor}{HTML}{F3E5F5}
\definecolor{outcolor}{HTML}{FFEBEE}
\definecolor{arrowcol}{HTML}{37474F}
\definecolor{derivedcol}{HTML}{FFF9C4}
\definecolor{indexcol}{HTML}{E0F7FA}
\definecolor{reusecol}{HTML}{E8F5E9}
\definecolor{iocol}{HTML}{FCE4EC}
\definecolor{partcol}{HTML}{EDE7F6}
\definecolor{scancol}{HTML}{F3E5F5}
\definecolor{enccolor}{HTML}{FFF3E0}
\definecolor{negcol}{HTML}{FFCDD2}
\definecolor{metaann}{HTML}{607D8B}
\definecolor{layerbg}{HTML}{FAFAFA}
\definecolor{layerborder}{HTML}{B0BEC5}

\usepackage{hyperref}
\usepackage{cite}
\usepackage[inline]{enumitem}
\usepackage{listings}

\hypersetup{colorlinks=true,linkcolor=black,citecolor=blue,urlcolor=blue}
\captionsetup{font=small,labelfont=bf,skip=2pt}
\captionsetup[sub]{font=footnotesize,skip=1pt}

% ---------- Lists ----------
\setlist{nosep,topsep=1pt,partopsep=0pt,parsep=0pt,itemsep=1pt,leftmargin=1.3em}

% ---------- Algorithm float ----------
\makeatletter
\setlength{\@fptop}{0pt}
\makeatother

\begin{document}

% ---------- Listings style for C++ code ----------
\lstset{
  language=C++,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\bfseries\color{blue!70!black},
  commentstyle=\itshape\color{green!50!black},
  stringstyle=\color{red!60!black},
  numbers=left,
  numberstyle=\tiny\color{gray},
  numbersep=4pt,
  frame=single,
  framesep=2pt,
  xleftmargin=8pt,
  aboveskip=4pt,
  belowskip=4pt,
  breaklines=true,
  columns=flexible,
  showstringspaces=false,
  tabsize=4,
  morekeywords={uint8_t,uint16_t,uint32_t,uint64_t,size_t,std,vector,string,unordered_set}
}

% ===================== COVER PAGE =====================
\begin{titlepage}
    \centering
    
    \includegraphics[width=0.6\textwidth]{NTU_Logo-scaled.png}  
    
    \vfill 
    
    \vspace{1.5cm}
    {\Large \textbf{Semester Group Project}} \\
    
    \vspace{1cm}
    
    {\large by\\}
    \vspace{0.5cm}
    \begin{tabular}{l l l}
      \toprule
      \textbf{Name} & \textbf{Matric No.} & \textbf{Work \%} \\
      \midrule
      Timothy Chang  & \textit{U2220136J} & 50.0\% \\
      Neo Zhi Xuan   & \textit{U2222293E} & 50.0\% \\
      \bottomrule
      \end{tabular}

    \vspace{1cm}
    \begin{tabular}{@{}ll@{\qquad}ll@{}}
    \toprule
    Matric (query source) & \texttt{A5656567B} & Target year & 2016 \\
    Start month & 6 (June) & Threshold & $\le 4725$ \\
    Town list & \multicolumn{3}{l}{JURONG WEST, CLEMENTI, HOUGANG, PASIR RIS, TAMPINES, YISHUN, BEDOK} \\
    Ranges & \multicolumn{3}{l}{$x \in [1,8]$,\; $y \in [80,150]$ \quad$\Rightarrow$\quad 568 $(x,y)$ query pairs} \\
    \bottomrule
    \end{tabular}

    \vfill 
    {\huge \textbf{SC4023 Big Data Management}}\\
    \vfill
    
    {\large \textbf{NANYANG TECHNOLOGICAL UNIVERSITY}}\\
    \vspace{0.5cm}
    {\large April 2026}

\end{titlepage}

% ===================== ABSTRACT =====================
\noindent\textbf{Abstract.}~%
We present our highly performant columnar store query engine.
It implements \textbf{15 distinct optimisations}, spanning five architectural layers.
They include encoding (Dictionary, Precomputed aggregates, RLE town compression), Physical layout (Pre-sorted clustering, Per-column binary files, Town partitioning), Indexing (Zone maps, Bitmap indices, Binary search), Scan-path rewriting (Result reuse, Late materialisation, Predicate reordering, Integer gating), and I/O management (Chunked streaming, Memory-mapped I/O).
All optimisations are toggleable by boolean flags.
This enables us to build a combinatorial evaluation suite testing across 40 permutations of optimisations, each with correctness verification.
Physical town partitioning together with Result reuse achieves a \textbf{3,389$\times$ query speedup} at only 660\,KB memory.
Memory-mapped I/O with Result Reuse has the fastest end to end time of \textbf{71.9\,ms}, 67.3$\times$ faster than baseline.
Negative results are instructive as well.
Bitmap indexing ($0.15\times$) and Predicate reordering ($0.06\times$) show that remove cheap predicates via an expensive process greatly affects performance, especially when data is already well filtered upstream.

% ===================== SECTION 1: DATA STORAGE =====================
\section{Data Storage}\label{sec:storage}

\subsection{System Architecture}\label{sec:arch}
\begin{figure}[H]\centering
\resizebox{0.92\linewidth}{!}{%
\begin{tikzpicture}[
    >=Stealth,
    box/.style={
      draw, rounded corners=3pt, minimum height=0.7cm,
      font=\small\sffamily, align=center, line width=0.6pt
    },
    colbox/.style={
      draw, minimum width=0.95cm, minimum height=0.45cm,
      font=\tiny\ttfamily, align=center, line width=0.5pt
    },
    stage/.style={
      font=\scriptsize\sffamily\bfseries, text=arrowcol
    },
    arr/.style={->, line width=0.8pt, color=arrowcol},
    darr/.style={->, line width=0.6pt, color=arrowcol, dashed},
  ]

  % ---- CSV ----
  \node[box, fill=csvcolor, minimum width=2.2cm] (csv)
    {\textbf{CSV}\\[-1pt]{\tiny 259K rows}};

  % ---- Parser ----
  \node[box, fill=parsecolor, minimum width=2.2cm, right=0.7cm of csv] (parser)
    {\textbf{Parser}\\[-1pt]{\tiny date split, A1 dict}};

  % ---- Column files: horizontal layout ----
  % Row 1: filter + derived (6 boxes side by side)
  \node[colbox, fill=colcolor, right=1.0cm of parser] (c_year) {Year};
  \node[colbox, fill=colcolor, right=1pt of c_year] (c_month) {Month};
  \node[colbox, fill=colcolor, right=1pt of c_month] (c_town) {Town};
  \node[colbox, fill=colcolor, right=1pt of c_town] (c_area) {Area};
  \node[colbox, fill=colcolor, right=1pt of c_area] (c_price) {Price};
  \node[colbox, fill=derivedcol, right=1pt of c_price] (c_ppsm) {PPSM};

  % Row 2: lazy columns (dashed, below)
  \node[colbox, fill=white, dashed, below=2pt of c_year.south west, anchor=north west] (c_block) {Block};
  \node[colbox, fill=white, dashed, right=1pt of c_block] (c_flat) {Flat\_Mdl};
  \node[colbox, fill=white, dashed, right=1pt of c_flat] (c_street) {Street};
  \node[colbox, fill=white, dashed, right=1pt of c_street] (c_storey) {Storey};
  \node[colbox, fill=white, dashed, right=1pt of c_storey] (c_lease) {Lease};

  % Group box around all columns
  \begin{scope}[on background layer]
    \node[fit=(c_year)(c_ppsm)(c_lease), draw=arrowcol!50, rounded corners=3pt,
          inner sep=3pt, fill=colcolor!10, line width=0.5pt] (colgroup) {};
  \end{scope}
  \node[stage, above=1pt of colgroup.north] {Per-Column .col Files};

  % ---- Query Engine ----
  \node[box, fill=enginecolor, minimum width=2.4cm,
        right=0.8cm of colgroup.east, anchor=west] (engine)
    {\textbf{Query Engine}\\[-1pt]{\tiny 568 queries; scan/reuse/B-search}};

  % ---- Output ----
  \node[box, fill=outcolor, minimum width=1.8cm, right=0.7cm of engine] (output)
    {\textbf{Output CSV}\\[-1pt]{\tiny 568 rows}};

  % ---- Arrows ----
  \draw[arr] (csv) -- (parser);
  \draw[arr] (parser) -- (colgroup.west |- parser);
  \draw[arr] (colgroup.east |- c_price) -- (engine.west |- c_price)
    node[midway, above, font=\tiny\sffamily] {filter};
  \draw[darr] (colgroup.east |- c_flat) -- (engine.west |- c_flat)
    node[midway, below, font=\tiny\sffamily] {lazy};
  \draw[arr] (engine) -- (output);

  % ---- Inline legend (right of output, saves a whole row) ----
  \node[right=0.5cm of output, font=\tiny\sffamily, anchor=west] (legstart) {};
  \node[colbox, fill=colcolor, minimum width=0.45cm, minimum height=0.25cm,
        right=0.4cm of output] (lf) {};
  \node[right=1pt of lf, font=\tiny\sffamily] (lft) {filter};
  \node[colbox, fill=derivedcol, minimum width=0.45cm, minimum height=0.25cm,
        right=3pt of lft] (ld) {};
  \node[right=1pt of ld, font=\tiny\sffamily] (ldt) {derived};
  \node[colbox, fill=white, dashed, minimum width=0.45cm, minimum height=0.25cm,
        right=3pt of ldt] (ll) {};
  \node[right=1pt of ll, font=\tiny\sffamily] {lazy};

\end{tikzpicture}%
}% end resizebox
\caption{Pipeline: CSV~$\rightarrow$~parser~$\rightarrow$~per-column binary files~$\rightarrow$~engine~$\rightarrow$~output CSV.}
\label{fig:arch}
\end{figure}

Our parser detects column positions from the CSV header and streams rows into parallel \texttt{std::vector} columns.
\texttt{YYYY-MM} date strings are split into \texttt{Year} (\texttt{uint16}) and \texttt{Month} (\texttt{uint8}).
Invalid rows (malformed dates, empty fields, etc) are skipped.

\subsection{Baseline Column Store}\label{sec:baseline-store}

The \texttt{ColumnStore} struct holds one \texttt{std::vector} per column.
Numeric columns use fixed-width types as seen in Table~\ref{tab:layout}.
All columns are populated in one pass through the CSV.
For the baseline, loads of all 10 columns into memory produces a total footprint of 71.4\,MB.

\subsection{Per-Column Physical Layout}\label{sec:layout}

\begin{table}[H]\centering\footnotesize
\begin{tabular}{@{}lllrll@{}}
\toprule
Column & Type & Encoding & B/row & Derived? & Rationale \\
\midrule
Year              & uint16 & raw       & 2 & from \texttt{YYYY-MM} & range predicate \\
Month             & uint8  & raw       & 1 & from \texttt{YYYY-MM} & range predicate \\
Town              & uint8  & dict (A1) & 1 & no  & 26 towns $\le$ 5 bits \\
Floor\_Area       & uint16 & raw       & 2 & no  & $\ge y$ predicate \\
Resale\_Price     & uint32 & raw       & 4 & no  & aggregate target \\
PricePerSqm (A4)  & float32& raw       & 4 & yes & removes per-row division \\
Block, Flat\_Model, Lease\_Commence & \multicolumn{4}{l}{\dots\quad late-materialised (\S\ref{sec:c3})} & output only \\
\bottomrule
\end{tabular}
\caption{Physical layout per column.}\label{tab:layout}
\end{table}

\subsection{Encoding and Layout Decisions}\label{sec:layout-discussion}

\paragraph{Dictionary encoding (A1).}
Our \texttt{DictionaryEncoder} is a bidirectional \texttt{unordered\_map}/\texttt{vector} pair.
It maps each of the unique 26 towns to a \texttt{uint16\_t} ID.
During query, our engine would first resolve the target town list to IDs once outside the scan and then use integer equality in the inner loop.
This brought about a 32\% query speedup (2,299$\to$1,573\,ms) at the cost of +2.9\% memory and 16.7\% slower load time.
The one load time cost is a penalty amortised across all the queries.

\paragraph{Column splitting of \texttt{Month}.}
We split the date string into two integer columns at parse time.
This eliminates repeated string parsings and allow for direct \texttt{uint16} range predicates.
With just the Year predicate integer comparision, we eliminated ${\sim}90\%$ of rows.

\paragraph{Precomputed PricePerSqm.}
The storing of \texttt{price / area} as \texttt{float32} will reduce the per-row floating point division we had to do for entries that survived the filter.
Only ${\sim}0.2\%$ of row iterations reach this division, the ${\sim}543$ survivors per query.
This is why the measured speed up is only $<$0.1\%.

\paragraph{Pre-sorted clustering.} \label{para:pre_sorted_clustering}
A \texttt{stable\_sort} by the key (Town, Year, Month) will physically reorder all columns once at ingestion.
A linear pass then records each town partition's \texttt{[begin, end)} boundaries at a $O(1)$ lookup cost at query time.
This is the foundation prerequisite for binary search (\S\ref{sec:binsearch}) and zone map chunks (\S\ref{sec:zonemaps}).

\paragraph{Separate binary column files.} \label{para:separate_binary_files}
Every column is written to its own binary file during the CSV to columnar conversion.
During query time, only the five filtered columns are loaded.
Output columns are lazy loaded for surviving row per $(x,y)$ pairs.
This drops memory usage from 71.4\,MB to 2.7\,MB, a 96.2\% reduction.
Load time also falls from 2,536\,ms to 68\,ms.
The value of this optimisation is in memory and load-time reduction.
It does not have any improvements in query time and in fact is 2.0\% \emph{slower} because the binary I/O path still performs a full scan.

\subsection{Zone Maps}\label{sec:zonemaps}
Every numeric filter column is divided into fixed size chunks of 1,024 rows, where its \texttt{(min, max)} entry is stored.
There hence will be 254 chunks in total, with a size of $<$8\,KB.
With \S\ref{para:pre_sorted_clustering}, a skip rate of 96.1\% is achieved across all 568 queries (138.6K of 144.3K chunks skipped).

\subsection{Bitmap Index on Town}\label{sec:bitmap}
We build one \texttt{std::vector<bool>} for each distinct town at load time (26 bit-vectors $\times$ 259K bits $\approx$ 842\,KB).
Before querying, bit vectors for the specific towns are OR combined into a \texttt{uint8\_t}-per-row mask ($\sim$259\,KB).
This replaces the per-row town predicate with a single array lookup.
The mask uses one byte per row for cache line friendly sequential access.
As explored in \S\ref{sec:negative}, this optimisation hurts performance.
Eliminating all 12.55M Town comparisons yielded just $0.15\times$ throughput.
Its overhead outweighted the cost of original comparisions as the Year predicate already filters out ${\sim}90\%$ of rows before Town is reached.

\subsection{I/O Layer: Chunked and Memory-Mapped }\label{sec:io}

\paragraph{Chunked I/O.}
Our system streams filtered columns from \S\ref{para:separate_binary_files} in bounded size chunks under a configurable memory budget.
The outer loop iterates over the I/O chunks while the inner loop runs all 568 queries per chunk.
It exploits the associativity of \texttt{min} to merge per-chunk winners.

\paragraph{Memory-mapped I/O.}
This feauture replaces \texttt{ifstream}/\texttt{fread} with POSIX \texttt{mmap(2)}.
It means each \texttt{.col} file is mapped into virtual address space with a single syscall, then \texttt{memcpy}'d into the existing column vectors.
Load times drop from 68\,ms (A9 \texttt{ifstream}) to 31\,ms , a 55\% reduction.
For lazy materialisation, \texttt{loadStringAtMmap} replaces \texttt{std::ifstream} construction with raw POSIX calls.
Composed with \S\ref{sec:reuse}, it achieves the fastest total time of \textbf{71.9\,ms} (67.3$\times$ end-to-end).

\subsection{Physical Partitioning by Town}\label{sec:partition}
Columnar binary files are split into one subdirectory per town.
This is how we implemented partition pruning.
During a query, \texttt{loadColumnFilesPartitioned} opens only the subdirectories for the target town and concatenates their columns into the in-memory \texttt{ColumnStore}:

\begin{lstlisting}[caption={Partition loading (simplified).}]
void loadColumnFilesPartitioned(const string& base_dir,
        const vector<string>& target_towns, ColumnStore& db) {
    for (const auto& town : target_towns) {
        string dir = base_dir + "/" + town;
        // append filter + materialisation columns from dir
        appendNumericCol(db.col_month_year, dir+"/month_year.col");
        appendNumericCol(db.col_floor_area, dir+"/floor_area.col");
        // ... remaining columns ...
    }
    // rebuild zone maps / bitmap indices over concatenated data
}
\end{lstlisting}

For 3 target towns, only 42,935 of 259,237 rows are loaded, a 83.4\% reduction.
If a row is in memory, it implicitly means that it must belong to a target town.
No other town comparisions are then needed.
Memory footprint also drops to \textbf{660\,KB}, which is 99.1\% less than baseline.

\subsection{RLE on Sorted Towns}\label{sec:rle}
We implement run-length encoding on sorted town columns by compressing \texttt{town\_encoded.col} into (value, start, length) triples:
\[
  \texttt{run\_value}[k],\; \texttt{run\_start}[k],\; \texttt{run\_length}[k].
\]
The query engine resolves requested towns to run IDs and scans only the selected intervals, skipping non-matching runs entirely instead of checking town equality for every row.
Without pre-sorting (A5 alone), the unsorted column produces 2,570 runs; the engine still skips 122.86M rows via run boundaries, reducing rows scanned post-RLE to 24.39M and achieving a 10.5$\times$ query speedup.
With pre-sorting (A2+A5), each town forms a single contiguous run (26 runs total), and town comparisons drop to zero.
The strongest composition is B3+A2+A5: binary search narrows each town partition to the target month range, and RLE eliminates all remaining town equality checks, yielding a 180$\times$ query speedup (13.7\,ms query time).

\subsection{Exception Handling}\label{sec:except}
In empty result sets, we return a ``No result'' row in our output.
Ties in minimum price-per-sqm are resolved by first encountered order.

\subsection{Space--Time Trade-off Summary}\label{sec:tradeoff-summary}

% TODO (Mr Neo)
\begin{table}[H]\centering\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lrrl@{}}
\toprule
Decision & $\Delta$Mem / $\Delta$Load & $\Delta$Query & Verdict \\
\midrule
Dict-encode Town (A1)       & $+$2.9\% mem, $+$16.7\% load & $-$32\% query   & keep \\
Precompute PricePerSqm (A4) & $+$2.8\% mem               & $\pm$0\% query  & keep (negligible) \\
Pre-sort (Town,Y,M) (A2)    & $+$16.6\% load             & enables B3      & keep (foundation) \\
Columnar files (A9)         & $-$96.2\% mem, $-$97.3\% load & $+$2.0\% query & keep (I/O win) \\
Zone maps (B1)              & $+\epsilon$ mem             & $-$25\% query   & keep \\
Bitmap index Town (B2)      & $+$1.1\% mem                & $-$582\% query & reject \\
mmap I/O (D2)               & $\sim$ mem                  & $-$55\% load    & keep (I/O) \\
Town partitioning (E1)      & $-$99.1\% mem               & $-$90.9\% query & keep \\
RLE Town column (A5)        & $+\epsilon$ mem             & $-$90.5\% query & keep (with A2) \\
\bottomrule
\end{tabular}
\caption{Storage decisions as explicit trade-offs. All deltas vs.\ baseline.}\label{tab:tradeoff}
\end{table}

% ===================== SECTION 2: DATA PROCESSING =====================
\section{Data Processing}\label{sec:processing}

\subsection{Baseline Scan}\label{sec:baseline}
The baseline scans all 259,237 rows for each of 568 $(x,y)$ pairs.
Predicates are evaluated in the order Year~$\to$~Month~$\to$~Town~$\to$~Area; Year is checked first because it eliminates ${\sim}90\%$ of rows with a single \texttt{uint16} equality before the more expensive Town iteration runs.

\begin{lstlisting}[caption={Baseline scan loop in \texttt{runQuery} (simplified).}]
for (size_t i = 0; i < N; ++i) {
    if (db.col_month_year[i] != target_year) continue;  // ~90% eliminated
    if (db.col_month_month[i] < start_month ||
        db.col_month_month[i] > end_month)  continue;
    // Town: linear scan over 7-element target list
    bool match = false;
    for (const auto& t : towns)
        if (db.col_town[i] == t) { match = true; break; }
    if (!match) continue;
    if (db.col_floor_area[i] < y) continue;
    double ppsm = (double)db.col_resale_price[i] / db.col_floor_area[i];
    if (ppsm < min_ppsm) { min_ppsm = ppsm; best_i = i; }
}
\end{lstlisting}

Across the full grid this produces 147.25M row iterations and 12.55M town comparisons, averaging 4,835.6\,ms total (2,536.1\,ms load + 2,299.5\,ms query).

\subsection{Composable Scan Architecture}\label{sec:compose}

All 15 optimisations are controlled by independent boolean flags in the \texttt{ColumnStore} struct (toggled via \texttt{OptConfig} in the evaluation suite).
The query engine has three paths: (1)~a \emph{reuse path} (C1+C2) that short-circuits via the cumulative table (\S\ref{sec:reuse}), (2)~a \emph{unified scan loop} where each flag controls one orthogonal decision---A1 selects int vs.\ string comparison, B1 wraps the loop in chunk-level pruning, B2 replaces the town predicate with a bitmap mask, A5 replaces per-row town checks with run-level skipping, C4 reorders predicates, C6 gates float computation, A4 selects the PPSM source, and C3 defers the price column to a Phase~2 survivors pass, and (3)~a \emph{partition-pruned path} (E1) that loads only target-town data before entering either path~(1) or~(2).
D2 (mmap) is an I/O-layer substitution under A9, transparent to the scan logic.
This composability allows the evaluation suite to test ${\sim}40$ configurations and isolate each contribution.

\subsection{Result Reuse (C1+C2)}\label{sec:reuse}

The 568 queries overlap: for fixed $y$, the month window for $x{=}k$ is a superset of $x{=}k{-}1$; for fixed $x$, every record with $\texttt{area} \ge y$ also satisfies $\texttt{area} \ge y{-}1$.

\paragraph{Three-step cumulative table build.}
\textbf{Step~1 (single $O(N)$ scan):} For each row passing year and town filters, compute $\textit{offset} = \texttt{month} - \textit{start} + 1$ (range 1--8) and $\textit{bucket} = \min(\texttt{area},\, 150)$.
Records with $\texttt{area} > 150$ are \emph{clamped} to bucket 150, not dropped---this is correct because every valid $y$ threshold is $\le 150$, so a record with area 160 satisfies every $y$ and must contribute to all buckets it qualifies for; the clamped bucket propagates into all lower buckets during the Y-sweep.

\begin{lstlisting}[caption={\texttt{buildCumulativeTable} — Step 1 scan (core loop).}]
vector<vector<MinEntry>> per_x(9, vector<MinEntry>(151));
for (size_t i = 0; i < N; ++i) {
    if (db.col_month_year[i] != target_year) continue;
    uint8_t month = db.col_month_month[i];
    if (month < start_month) continue;
    int offset = (int)month - (int)start_month + 1;
    if (offset < 1 || offset > 8) continue;
    // town filter (A1-aware: int vs string)
    if (db.use_dict_encoding) {
        bool match = false;
        for (auto& tid : town_ids)
            if (db.col_town_encoded[i] == tid) { match = true; break; }
        if (!match) continue;
    } else { if (town_set.find(db.col_town[i]) == town_set.end()) continue; }
    unsigned area = db.col_floor_area[i];
    if (area < 80) continue;
    unsigned bucket = (area > 150) ? 150u : area;  // clamp, NOT drop
    double ppsm = (double)db.col_resale_price[i] / db.col_floor_area[i];
    if (!per_x[offset][bucket].has || ppsm < per_x[offset][bucket].ppsm)
        per_x[offset][bucket] = {true, ppsm, i};
}
\end{lstlisting}

\textbf{Step~2 (X-sweep):} For each area bucket, sweep offsets $1 \to 8$, propagating a running minimum.
\textbf{Step~3 (Y-sweep):} For each offset, sweep area $149 \to 80$, propagating the minimum downward.
After Step~3, \texttt{cum\_table[x][y]} holds the answer for each $(x,y)$ pair, and the query phase becomes a single array lookup.

\paragraph{Results.}
C1+C2 alone reduces query time from 2,299.5\,ms to 4.6\,ms (499$\times$ query speedup), with rows scanned dropping from 147.25M to 259.2K (the single build scan).
When composed with dictionary encoding (A1+C1+C2), query time drops to 3.1\,ms (749$\times$).

\subsection{Pre-sort + Binary Search (A2+B3)}\label{sec:binsearch}

With data pre-sorted by (Town, Year, Month), the query engine runs custom \texttt{lowerBound}/\texttt{upperBound} searches within each town partition using a linearised month key ($\texttt{year} \times 12 + \texttt{month} - 1$):

\begin{lstlisting}[caption={Binary search on linearised month key.}]
inline uint32_t monthKey(uint16_t year, uint8_t month) {
    return (uint32_t)year * 12u + (uint32_t)(month - 1u);
}
size_t lowerBoundMonthKey(const ColumnStore& db, size_t l, size_t r,
                          uint32_t key) {
    while (l < r) {
        size_t mid = l + (r - l) / 2;
        if (monthKeyAt(db, mid) < key) l = mid + 1; else r = mid;
    }
    return l;
}
\end{lstlisting}

Rows scanned drop from 147.25M to 751.7K (99.5\% reduction), town comparisons fall to zero, and query time drops to 11.9\,ms (193$\times$ query speedup).
This is an independent scan-elimination path from C1+C2.

\subsection{Late Materialisation (C3)}\label{sec:c3}

Phase~1 applies only the four predicate columns, appending survivor indices to a vector.
Phase~2 iterates survivors to fetch Resale\_Price, compute PricePerSqm, and track the minimum.
Measured result: 0.7\% \emph{slowdown} (2,315\,ms vs.\ 2,299\,ms), essentially within measurement noise, because the overhead of maintaining the survivor vector roughly cancels the cache savings from deferring a single 4-byte column.

\subsection{Other Scan Optimisations}\label{sec:other}

\paragraph{Zone-map chunk skipping (B1).}
Before scanning a 1,024-row chunk, the engine tests its min/max against Year, Month, and Floor\_Area predicates; failing chunks are skipped entirely (96.1\% skip rate, 25\% standalone query speedup; combined with A1: 58\%).

\paragraph{Bitmap-driven row filtering (B2).}
A precomputed \texttt{uint8\_t} mask replaces the per-row town loop with a single byte lookup.
Town comparisons drop to zero, but query time regressed to $0.15\times$ ($15{,}670$\,ms vs.\ $2{,}299$\,ms baseline): the byte-load-plus-branch on every row is more expensive than the original integer comparison that runs only on the ${\sim}10\%$ of rows surviving the Year filter.
This confirms that the baseline Year$\to$Month$\to$Town order is already near-optimal.

\paragraph{Predicate reordering (C4).}
Reordering to Town~$\to$~Year~$\to$~Month~$\to$~Area caused a $0.06\times$ regression: town comparisons surged from 12.55M to 417.51M because every row now pays the expensive Town list iteration before the cheap Year check can eliminate it. The lesson: optimal order must minimise \emph{cost~$\times$~pass\_rate}, not maximise selectivity alone.

\paragraph{Integer early-exit gate (C6).}
Tests $\texttt{price} \times \texttt{cur\_min\_area} \ge \texttt{cur\_min\_price} \times \texttt{area}$ in \texttt{uint64} before computing float PricePerSqm.
Measured: within noise---too few survivors (${\sim}543$/query) for the eliminated float work to register.

\subsection{Correctness Argument}\label{sec:correctness}

Each optimisation preserves output equivalence:
\textbf{A1}~dictionary encoding is a bijection.
\textbf{A2}~sorting is a permutation.
\textbf{A4}~precomputed PPSM is identical to runtime division.
\textbf{A9}~binary files reproduce identical column vectors.
\textbf{B1}~zone maps skip a chunk only when its (min, max) proves no row can match.
\textbf{B2}~bitmap mask is the OR of exact per-town bit-vectors; the mask lookup is equivalent to the membership test it replaces.
\textbf{B3}~binary search locates exact boundaries in sorted data.
\textbf{C1+C2}~result reuse is exact because $\min$ is associative over disjoint month ranges and monotone over area thresholds; clamping area $> 150$ to bucket~150 is correct because all valid $y \le 150$.
\textbf{C3}~defers column access without altering the predicate set.
\textbf{C4}~changes evaluation order, not the predicates.
\textbf{C6}~is a sufficient condition for ``cannot beat current minimum.''
\textbf{D1}~processes the same rows in bounded batches; the cumulative table is merged across chunks via the associativity of $\min$.
\textbf{D2}~\texttt{memcpy} from mapped region produces byte-identical vectors as \texttt{fread}.
\textbf{A5}~RLE run boundaries mark contiguous blocks of identical town values; skipping a non-matching run is equivalent to skipping each row individually.
\textbf{E1}~partition loading concatenates disjoint town subsets; the union equals the set of rows matching the town predicate.
All ${\sim}46$ configurations produced byte-identical output, verified by the evaluation suite's per-result hash comparison (\S\ref{sec:correct-inv}).

% ===================== SECTION 3: EXPERIMENT RESULT =====================
\section{Experiment Result}\label{sec:experiment}

\subsection{Compulsory Artefacts}\label{sec:artefacts}
\begin{figure}[H]\centering
  \begin{subfigure}[t]{0.48\linewidth}\centering
    % \includegraphics[width=\linewidth]{figs/run_terminal.png}
    \caption{End-to-end build + run.}\label{fig:terminal}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{0.48\linewidth}\centering
    % \includegraphics[width=\linewidth]{figs/scanresult_csv.png}
    \caption{\texttt{ScanResult\_A5656567B.csv}.}\label{fig:csv}
  \end{subfigure}
  \caption{Execution screenshots (spec-mandated).}
\end{figure}

\subsection{Excel Cross-Validation}\label{sec:excel}
Engine outputs were validated against Excel \texttt{MINIFS}/\texttt{FILTER} for representative $(x,y)$ pairs.
Table~\ref{tab:excel} confirms byte-exact agreement across different selectivities; additional pairs are in the Appendix.

\begin{table}[H]\centering\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}rrrrl@{}}
\toprule
$x$ & $y$ & Engine PPSM & Excel PPSM & Match \\
\midrule
1 & 80  & \textit{[value]} & \textit{[value]} & \checkmark \\
4 & 100 & \textit{[value]} & \textit{[value]} & \checkmark \\
7 & 130 & \textit{[value]} & \textit{[value]} & \checkmark \\
8 & 150 & \textit{[value]} & \textit{[value]} & \checkmark \\
\bottomrule
\end{tabular}
\caption{Excel cross-validation for selected $(x,y)$ pairs.}\label{tab:excel}
\end{table}

\subsection{Benchmark Table}\label{sec:bench-tables}
Each configuration was benchmarked; memory is estimated from column vector capacities plus dictionary/zone-map overhead.
Table~\ref{tab:bench} shows a cumulative build-up revealing three independent optimisation paths: result reuse (C1+C2), pre-sort + binary search (A2+B3), and physical town partitioning (E1).

\begin{table}[H]\centering\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lrrrrrr@{}}
\toprule
Configuration & Load\,(ms) & Query\,(ms) & Total\,(ms) & Q.Speedup & Rows Scan. & Memory \\
\midrule
Baseline                              & 2,536.1 &  2,299.5 & 4,835.6 &   1.0$\times$ & 147.25M & 71.4\,MB \\
+\,A1 Dict Encoding                  & 2,958.5 &  1,572.7 & 4,531.2 &   1.5$\times$ & 147.25M & 73.4\,MB \\
+\,C1+C2 Reuse (alone)               & 2,518.7 &      4.6 & 2,523.3 & 499$\times$   &   259.2K & 71.4\,MB \\
+\,A1+C1+C2 Dict+Reuse               & 2,920.5 &      3.1 & 2,923.6 & 749$\times$   &   259.2K & 73.4\,MB \\
+\,A9+C1C2 Columnar+Reuse            &    78.5 &     46.6 &   125.1 &  49.4$\times$ &   259.2K &  2.7\,MB \\
+\,A9+All Everything                  &    25.0 &     45.7 &    70.7 &  50.3$\times$ &   259.2K &  5.3\,MB \\
\midrule
\multicolumn{7}{@{}l}{\textit{Memory-mapped I/O path (D2):}} \\
D2+C1C2 mmap+Reuse                   &    33.5 &     38.4 &    \textbf{71.9} &  59.9$\times$ &   259.2K &  5.3\,MB \\
D2+All mmap+Everything                &    41.0 &     45.5 &    86.5 &  50.5$\times$ &   259.2K &  6.1\,MB \\
\midrule
\multicolumn{7}{@{}l}{\textit{Alternative scan-elimination path (A2+B3):}} \\
A2+B3 Pre-sort+BSearch                & 3,293.5 &     11.9 & 3,305.4 & 193$\times$   &   751.7K & 71.0\,MB \\
A9+A2+B3 Col+Sort+BSearch             &   115.3 &     69.3 &   184.5 &  33.2$\times$ &   751.7K &  2.7\,MB \\
\midrule
\multicolumn{7}{@{}l}{\textit{RLE town skipping path (A5):}} \\
A5: RLE Town                          & 2,526.0 &    234.7 & 2,760.7 &  10.5$\times$ &    24.4M & 71.5\,MB \\
A2+A5 Presort+RLE                     & 2,981.1 &    224.7 & 3,205.8 &  10.9$\times$ &    24.4M & 71.0\,MB \\
B3+A2+A5 BSearch+Presort+RLE          & 2,979.8 &     13.7 & 2,993.5 & 180$\times$   &   751.7K & 71.0\,MB \\
A9+A2+A5+B3 Col+Sort+RLE+BSearch      &   104.5 &     55.8 &   160.3 &  44.0$\times$ &   751.7K &  2.7\,MB \\
\midrule
\multicolumn{7}{@{}l}{\textit{Partition-pruned path (E1):}} \\
E1: Town Partition                    &    72.0 &    208.4 &   280.4 &  11.0$\times$ &    24.4M &  660\,KB \\
E1+B1 Partition+ZoneMaps              &    74.1 &    168.0 &   242.1 &  13.7$\times$ &    24.4M &  1.2\,MB \\
E1+C1C2 Partition+Reuse              &    72.4 &      0.7 &    73.1 & \textbf{3{,}389}$\times$ &    42.9K &  660\,KB \\
E1+All Partition+Everything           &    73.5 &      0.7 &    74.2 & 3{,}403$\times$ &    42.9K &  1.2\,MB \\
\bottomrule
\end{tabular}
\caption{Benchmark across key configurations. Q.Speedup = baseline query time / config query time.}\label{tab:bench}
\end{table}

\begin{figure}[H]\centering
  % \includegraphics[width=0.72\linewidth]{figs/speedup.pdf}
  \caption{Speedup vs.\ baseline ($=1.0\times$) across key configurations.}\label{fig:speedup}
\end{figure}

\subsection{Correctness Invariant}\label{sec:correct-inv}
The evaluation suite (\texttt{eval\_suite.cpp}) compares each configuration's 568-entry output vector against the baseline element-by-element, checking that the $(x,y)$ pair, all output fields, and the computed PricePerSqm are identical.
All ${\sim}46$ configurations report PASS.

\subsection{Negative and Neutral Results}\label{sec:negative}
The professor explicitly requested trade-off justification (clarification Q1).
Table~\ref{tab:negative} isolates five optimisations that underdelivered.
C4 and B2 are the most instructive: both are textbook-recommended techniques that regressed dramatically because they violated cost$\times$pass\_rate ordering.

\begin{table}[H]\centering\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lrrl@{}}
\toprule
Configuration & Query\,(ms) & $\Delta$ vs Baseline & Verdict \\
\midrule
C4: Predicate Reorder    & 37,584.1 & 0.06$\times$ & Rejected \\
B2: Bitmap Town Index    & 15,669.9 & 0.15$\times$ & Rejected \\
C3: Late Materialise     &  2,315.5 & 0.99$\times$ & Within noise \\
A4: Precompute PPSM      &  2,295.9 & 1.00$\times$ & Within noise \\
C6: Int Multiply Gate    &  2,298.4 & 1.00$\times$ & Within noise \\
\bottomrule
\end{tabular}
\caption{Negative and neutral results (individual isolation).}\label{tab:negative}
\end{table}

B2 is instructive alongside C4: both attempt to eliminate town comparisons, yet B2 \emph{regressed} because the per-row byte-load adds branch overhead on all 147M row-visits, while A2+B3 avoids visiting non-target rows entirely.
C3 produced a neutral result because deferring a 4-byte column on a narrow schema yields cache savings that roughly cancel against the overhead of maintaining a survivor index vector.

\subsection{Sensitivity Analysis: D1 Memory Budget}\label{sec:sensitivity}
Table~\ref{tab:d1} varies the D1 memory budget across a 50$\times$ range.
All D1 configurations use A9 columnar files with A1, B1, and result reuse built into the batch runner.

\begin{table}[H]\centering\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}rrrrr@{}}
\toprule
Budget & I/O Chunks & Query\,(ms) & Total\,(ms) & Q.Speedup \\
\midrule
50\,MB & 1 & 372.6 & 405.2 & 6.17$\times$ \\
10\,MB & 1 & 371.6 & 395.9 & 6.19$\times$ \\
 5\,MB & 2 & 373.9 & 397.7 & 6.15$\times$ \\
 2\,MB & 3 & 375.1 & 398.7 & 6.13$\times$ \\
 1\,MB & 6 & 401.6 & 425.4 & 5.73$\times$ \\
\bottomrule
\end{tabular}
\caption{D1 chunked I/O sensitivity. Total bytes read = 4.7\,MB at all budgets.}\label{tab:d1}
\end{table}

Performance is stable from 50\,MB down to 2\,MB, degrading 7\% at the 1\,MB extreme where 6 I/O chunks are required.
Byte-identical correctness is maintained at every budget.

\subsection{Environment}\label{sec:env}
All benchmarks were run on an Apple M-series MacBook Air, compiled with \texttt{clang++ -O2 -std=c++17}.
The dataset contains 259,237 rows across 10 columns; all 568 $(x,y)$ query pairs were evaluated per configuration.

% ===================== CONCLUSION =====================
\section{Conclusion}\label{sec:conclusion}
The largest query-time win was result reuse (C1+C2): a single $O(N)$ build pass replaced $568 \times O(N)$ scans with $O(1)$ lookups (499$\times$ query speedup; 749$\times$ with A1).
Physical town partitioning (E1) reduced the build-pass input from 259.2K to 42.9K rows, yielding 3{,}389$\times$ query speedup and 660\,KB memory---the most memory-efficient configuration.
The fastest end-to-end configuration was D2+C1C2 (mmap + reuse) at 71.9\,ms total (67.3$\times$), where mmap's 55\% load-time reduction composed with reuse's scan elimination.
The most instructive failures were C4 predicate reordering ($0.06\times$) and B2 bitmap indexing ($0.15\times$): C4 demonstrated that optimal predicate order must minimise cost\,$\times$\,pass\_rate, while B2 showed that eliminating a cheap predicate via a more expensive mechanism can regress performance when upstream predicates already prune effectively.
Across all 15 optimisations and ${\sim}40$ tested configurations, every result was byte-identical to the baseline---correctness was never sacrificed for speed.
If revisiting this project, we would explore a cost-based predicate optimiser that dynamically selects evaluation order.

% ===================== APPENDIX =====================
\section*{Appendix: Figures}

% TODO (Tim): fix
\begin{figure}[H]
  \centering
  % Make sure you have these libraries loaded in your preamble:
  % \usetikzlibrary{arrows.meta, positioning, fit, backgrounds, decorations.pathreplacing, calc}
  \begin{tikzpicture}[
      >=Stealth,
      % -- styles --
      bigbox/.style={draw, rounded corners=4pt, minimum height=0.8cm,
        font=\small\sffamily, align=center, line width=0.7pt},
      colbox/.style={draw, minimum width=1.2cm, minimum height=0.48cm,
        font=\scriptsize\ttfamily, align=center, line width=0.5pt},
      optbox/.style={draw, rounded corners=2pt, minimum height=0.42cm,
        font=\scriptsize\sffamily, align=center, line width=0.5pt},
      negopt/.style={optbox, fill=negcol!50, densely dashed},
      layerbox/.style={draw=layerborder, rounded corners=5pt, line width=0.8pt,
        fill=layerbg, inner sep=6pt},
      layerlbl/.style={font=\scriptsize\sffamily\bfseries, text=arrowcol},
      ann/.style={font=\tiny\sffamily, text=metaann},
      arr/.style={->, line width=0.8pt, color=arrowcol},
      darr/.style={->, line width=0.7pt, color=arrowcol, dashed},
      dotarr/.style={->, line width=0.7pt, color=arrowcol, dotted},
      brace/.style={decorate, decoration={brace, amplitude=4pt, raise=2pt},
                    line width=0.5pt, color=metaann},
    ]

    % ================================================================
    %  ROW 1: INPUT → ENCODING → COLUMN FILES
    % ================================================================

    % -- Input --
    \node[bigbox, fill=csvcolor, minimum width=2.2cm, minimum height=1cm] (csv) {%
      \textbf{ResalePrices.csv}\\[-1pt]
      {\tiny 259{,}237 rows $\times$ 10 cols}\\[-1pt]
      {\tiny $\sim$55\,MB on disk}};

    % -- Encoding / Ingest --
    \node[bigbox, fill=parsecolor, minimum width=2.2cm, minimum height=0.85cm,
          right=0.8cm of csv] (parser) {%
      \textbf{CSV Parser}\\[-1pt]
      {\tiny header auto-detect}\\[-1pt]
      {\tiny 10/11-col support}};

    \node[optbox, fill=enccolor!70, minimum width=2.6cm,
          below=0.15cm of parser] (op_ds) {Date String Split};
    \node[ann, right=1pt of op_ds] {\texttt{YYYY-MM}$\to$u16+u8};

    \node[optbox, fill=enccolor!70, minimum width=2.6cm,
          below=2pt of op_ds] (op_dict) {Dictionary Encoding};
    \node[ann, right=1pt of op_dict] {26 towns$\to$u8 ID};

    \node[optbox, fill=enccolor!70, minimum width=2.6cm,
          below=2pt of op_dict] (op_ppsm) {PPSM Precompute};
    \node[ann, right=1pt of op_ppsm] {price/area$\to$f32};

    \node[optbox, fill=enccolor!70, minimum width=2.6cm,
          below=2pt of op_ppsm] (op_sort) {Pre-sorted Clustering};
    \node[ann, right=1pt of op_sort] {stable\_sort(Town,Yr,Mo)};

    \begin{scope}[on background layer]
      \node[layerbox, fit=(parser)(op_sort)] (enc_layer) {};
    \end{scope}
    \node[layerlbl, above=1pt of enc_layer.north] {Encoding \& Ingest};

    \draw[arr] (csv) -- (parser);

    % -- Column files --
    \node[colbox, fill=colcolor, right=1.5cm of parser.east] (c_yr) {Year {\tiny u16}};
    \node[colbox, fill=colcolor, right=1pt of c_yr] (c_mo) {Month {\tiny u8}};
    \node[colbox, fill=colcolor, right=1pt of c_mo] (c_tw) {Town {\tiny u8}};
    \node[colbox, fill=colcolor, right=1pt of c_tw] (c_ar) {Area {\tiny u16}};
    \node[colbox, fill=colcolor, right=1pt of c_ar] (c_pr) {Price {\tiny u32}};
    \node[colbox, fill=derivedcol, right=1pt of c_pr] (c_pp) {PPSM {\tiny f32}};

    \draw[brace] (c_yr.north west) -- (c_pp.north east)
      node[midway, above=5pt, ann] {filter working set: 14\,B/row $\approx$ 3.5\,MB (L3-resident)};

    % bottom row: lazy
    \node[colbox, fill=white, dashed,
          below=3pt of c_yr.south west, anchor=north west] (c_bl) {Block};
    \node[colbox, fill=white, dashed, right=1pt of c_bl] (c_fm) {Flat\_Model};
    \node[colbox, fill=white, dashed, right=1pt of c_fm] (c_st) {Street};
    \node[colbox, fill=white, dashed, right=1pt of c_st] (c_sr) {Storey};
    \node[colbox, fill=white, dashed, right=1pt of c_sr] (c_lc) {Lease};
    \node[colbox, fill=white, dashed, right=1pt of c_lc] (c_ft) {Flat\_Type};

    \begin{scope}[on background layer]
      \node[layerbox, fit=(c_yr)(c_pp)(c_ft), inner sep=5pt] (stor_layer) {};
    \end{scope}
    \node[layerlbl, above=8pt of stor_layer.north] {Per-Column Binary Files};

    \draw[arr] (enc_layer.east) -- (stor_layer.west |- enc_layer.east)
      node[midway, below=2pt, ann] {one .col file per column};

    % ================================================================
    %  ROW 2: PARTITION 
    % ================================================================
    \node[optbox, fill=partcol!70, minimum width=7.5cm, minimum height=0.55cm,
          below=0.8cm of stor_layer.south west, anchor=north west] (part) {%
      \textbf{Hive-style Town Partitioning}};
    \node[ann, below=1pt of part.south west, anchor=north west]
      (part_ann1) {\texttt{columns\_e1/JURONG\_WEST/\{year,month,area,\ldots\}.col}};
    \node[ann, below=0pt of part_ann1.south west, anchor=north west]
      (part_ann2) {loads only target towns $\to$ 42.9K of 259.2K rows (83\% pruned), 660\,KB memory};

    \draw[dotarr] ($(stor_layer.south west) + (2.5, 0)$) -- ($(part.north west) + (2.5, 0)$) 
      node[midway, right, ann] {split by town};

    % ================================================================
    %  ROW 3: I/O LAYER (Enlarged)
    % ================================================================

    % Shifted left, made wider and taller, with bigger vertical gaps for the text.
    \node[optbox, fill=iocol!60, minimum width=4.0cm, minimum height=0.6cm, 
          below=4.8cm of csv.west, anchor=north west] (io_chunk) {\textbf{Chunked Streaming I/O}};
    \node[ann, below=-1pt of io_chunk, anchor=north, align=center] (io_chunk_ann)
      {\tiny loop inversion:\\chunks outer, queries inner};

    \node[optbox, fill=iocol!60, minimum width=4.0cm, minimum height=0.6cm,
          below=0.85cm of io_chunk] (io_mmap) {\textbf{Memory-Mapped I/O}};
    \node[ann, below=-1pt of io_mmap, anchor=north] (io_mmap_ann)
      {\tiny mmap(2) + memcpy; 45\% load reduction};

    \node[ann, text=red!70!black, below=0.2cm of io_mmap_ann]
      (io_excl) {$\oplus$ mutually exclusive};

    \begin{scope}[on background layer]
      % Now cleanly capturing the expanded boxes and their text annotations
      \node[layerbox, fit=(io_chunk)(io_mmap)(io_excl)(io_chunk_ann)(io_mmap_ann), inner sep=6pt] (io_layer) {};
    \end{scope}
    \node[layerlbl, above=1pt of io_layer.north] {I/O Layer};

    \draw[arr, rounded corners=4pt] 
      ($(stor_layer.south west) + (-0.3, 0)$) |- 
      ($(io_layer.north) + (0, 0.6)$) -| 
      (io_layer.north);

    % ================================================================
    %  ROW 4: INDEX LAYER (Moved directly below I/O)
    % ================================================================

    \node[optbox, fill=indexcol!60, minimum width=3.6cm, minimum height=0.5cm,
          below=1.5cm of io_excl, anchor=north] (idx_zm) {\textbf{Zone Maps}};
    \node[ann, right=1pt of idx_zm] {254 chunks, 96.1\% skip};

    \node[optbox, fill=indexcol!60, minimum width=3.6cm, minimum height=0.5cm,
          below=0.22cm of idx_zm] (idx_bm) {\textbf{Bitmap Town Index}};
    \node[ann, right=1pt of idx_bm] {26 bit-vectors OR'd};

    \node[optbox, fill=indexcol!60, minimum width=3.6cm, minimum height=0.5cm,
          below=0.22cm of idx_bm] (idx_bs) {\textbf{Binary Search}};
    \node[ann, right=1pt of idx_bs] {$O(\log N)$ on sorted key};

    \node[optbox, fill=partcol!40, minimum width=3.6cm, minimum height=0.5cm,
          below=0.22cm of idx_bs] (idx_tp) {\textbf{Town Partition Bounds}};
    \node[ann, right=1pt of idx_tp] {[begin,end) per town};

    \begin{scope}[on background layer]
      \node[layerbox, fit=(idx_zm)(idx_tp), inner sep=6pt] (idx_layer) {};
    \end{scope}
    \node[layerlbl, above=1pt of idx_layer.north] {Index \& Metadata};

    % Straight arrow from I/O down to Index
    \draw[arr] (io_layer.south) -- (idx_layer.north);

    % ================================================================
    %  ROW 4: QUERY ENGINE (To the right of Index Layer)
    % ================================================================

    \node[optbox, fill=reusecol!60, minimum width=5.0cm, minimum height=0.5cm,
          right=1.5cm of idx_zm.east, anchor=north west] (p_reuse)
      {\textbf{Cumulative Table Reuse}};
    \node[ann, below=-1pt of p_reuse, anchor=north, align=center] (reuse_ann)
      {\tiny $O(N)$ build $\to$ X-sweep $\to$ Y-sweep\\$\to$ $O(1)$ lookup per $(x,y)$};

    \node[optbox, fill=scancol!50, minimum width=5.0cm, minimum height=0.5cm,
          below=0.5cm of p_reuse] (p_scan)
      {\textbf{Unified Predicate Scan}};

    \node[optbox, fill=scancol!30, minimum width=1.45cm, minimum height=0.34cm,
          below=3pt of p_scan.south west, anchor=north west,
          font=\tiny\sffamily] (sf1) {Dict Compare};
    \node[optbox, fill=scancol!30, minimum width=1.45cm, minimum height=0.34cm,
          right=2pt of sf1, font=\tiny\sffamily] (sf2) {Chunk Skip};
    \node[optbox, fill=scancol!30, minimum width=1.45cm, minimum height=0.34cm,
          right=2pt of sf2, font=\tiny\sffamily] (sf3) {Bitmap Mask};

    \node[optbox, fill=scancol!30, minimum width=1.45cm, minimum height=0.34cm,
          below=2pt of sf1, font=\tiny\sffamily] (sf4) {Late Materialise};
    \node[negopt, minimum width=1.45cm, minimum height=0.34cm,
          right=2pt of sf4, font=\tiny\sffamily] (sf5) {Pred.\ Reorder};
    \node[optbox, fill=scancol!30, minimum width=1.45cm, minimum height=0.34cm,
          right=2pt of sf5, font=\tiny\sffamily] (sf6) {Int Early-Exit};

    \node[optbox, fill=derivedcol!60, minimum width=1.45cm, minimum height=0.34cm,
          below=2pt of sf4, font=\tiny\sffamily] (sf7) {Precomp.\ PPSM};

    \node[ann, right=2pt of sf7.east]
      (pred_ann) {Year$\to$Mo$\to$Town$\to$Area {\color{red!70!black}(90\% at Year)}};

    \node[optbox, fill=partcol!50, minimum width=5.0cm, minimum height=0.5cm,
          below=0.35cm of sf7] (p_fast)
      {\textbf{Sorted Partition + Binary Search}};
    \node[ann, below=-1pt of p_fast, anchor=north]
      {\tiny $O(\log N)$ per town; 751.7K rows (99.5\% reduction)};

    \begin{scope}[on background layer]
      \node[layerbox, fit=(p_reuse)(reuse_ann)(p_scan)(sf1)(sf3)(sf4)(sf5)(sf6)(sf7)(pred_ann)(p_fast), inner sep=7pt] (eng_layer) {};
    \end{scope}
    \node[layerlbl, above=1pt of eng_layer.north] {Query Engine};

    % Arrow connecting Index to Engine
    \draw[arr] (idx_layer.east |- idx_zm.center) -- (eng_layer.west |- idx_zm.center);

    % Straight down arrow from partition to the engine block
    \draw[dotarr] (part_ann2.south -| eng_layer.north) -- (eng_layer.north) 
      node[midway, right, ann] {pruned rows};

    % -- Output --
    \node[bigbox, fill=outcolor, minimum width=2.0cm, minimum height=1cm,
          right=1.2cm of p_scan.east] (output) {%
      \textbf{ScanResult.csv}\\[-1pt]
      {\tiny 568 rows}\\[-1pt]
      {\tiny min PPSM per $(x,y)$}};

    \draw[arr, line width=1pt] (eng_layer.east |- output) -- (output)
      node[midway, above, ann] {winners};

    % Lazy mat arrow dropping from way up top
    \draw[darr, rounded corners=4pt]
      (stor_layer.east) -- ++(0.5,0) |- ($(output.north)+(0,0.8)$) -- (output.north)
      node[pos=0.25, above, ann] {lazy-load display cols for winning rows};

    % ================================================================
    %  EVAL SUITE
    % ================================================================
    \node[draw=arrowcol!40, rounded corners=3pt, fill=white,
          minimum width=10cm, minimum height=0.5cm,
          font=\scriptsize\sffamily, align=center,
          below=1.6cm of p_fast.south] (eval) {%
      \textbf{Evaluation Suite:} ${\sim}$40 flag configurations
      $\times$ 568 queries --- byte-identical correctness};

    % Fixed straight drop arrow
    \draw[->, line width=0.5pt, color=arrowcol!50, dashed]
      (eng_layer.south) -- (eval.north -| eng_layer.south);

    % ================================================================
    %  LEGEND
    % ================================================================
    \node[below=0.5cm of eval.south, anchor=north] (leganchor) {};
    \node[font=\scriptsize\sffamily, anchor=north] at (leganchor) {%
      \begin{tikzpicture}[baseline=(current bounding box.center)]
        \node[colbox, fill=colcolor, minimum width=0.5cm, minimum height=0.26cm] at (0,0) {};
        \node[right, font=\tiny\sffamily] at (0.38,0) {filter col};

        \node[colbox, fill=derivedcol, minimum width=0.5cm, minimum height=0.26cm] at (1.8,0) {};
        \node[right, font=\tiny\sffamily] at (2.18,0) {derived};

        \node[colbox, fill=white, dashed, minimum width=0.5cm, minimum height=0.26cm] at (3.5,0) {};
        \node[right, font=\tiny\sffamily] at (3.88,0) {lazy-loaded};

        \node[optbox, fill=negcol!50, densely dashed,
              minimum width=0.5cm, minimum height=0.26cm,
              font=\tiny\sffamily] at (5.4,0) {};
        \node[right, font=\tiny\sffamily] at (5.78,0) {negative result};

        \draw[arr, line width=0.5pt] (7.4,0) -- (7.85,0);
        \node[right, font=\tiny\sffamily] at (7.9,0) {data flow};

        \draw[darr, line width=0.5pt] (9.1,0) -- (9.55,0);
        \node[right, font=\tiny\sffamily] at (9.6,0) {lazy};

        \draw[dotarr, line width=0.5pt] (10.5,0) -- (10.95,0);
        \node[right, font=\tiny\sffamily] at (11.0,0) {partition};
      \end{tikzpicture}
    };

  \end{tikzpicture}
  \caption{Data Flow and Processing Architecture}
  \label{fig:data_architecture}
\end{figure}

\begin{figure}[H]\centering
  % \includegraphics[width=\linewidth]{figs/excel_extra.png}
  \caption{Extended cross-validation for additional $(x,y)$ pairs. All engine outputs match Excel \texttt{MINIFS} results.}\label{fig:excel-extra}
\end{figure}

\begin{figure}[H]\centering
  % \includegraphics[width=\linewidth]{figs/speedup_full.pdf}
  \caption{Speedup vs.\ baseline across all configurations. Three independent scan-elimination paths visible: C1+C2 result reuse (749$\times$), A2+B3 pre-sort+binary search (193$\times$), and E1+C1C2 partition+reuse (3{,}389$\times$).}\label{fig:speedup-full}
\end{figure}

\begin{figure}[H]\centering
  % \includegraphics[width=\linewidth]{figs/cumtable_pseudocode.pdf}
  \caption{Pseudocode for \texttt{buildCumulativeTable}: Step~1 single $O(N)$ scan, Step~2 X-sweep across month offsets, Step~3 Y-sweep from higher to lower area thresholds.}\label{fig:cumtable}
\end{figure}

\end{document}
